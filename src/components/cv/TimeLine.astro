---
const { title, subtitle, desc, honors, average } = Astro.props;
---

<div class="flex">
  <div class="education__time">
    <span class="w-4 h-4 bg-primary block rounded-full mt-1"></span>
    <span class="education__line bg-primary block h-full w-[2px] translate-x-[7px]"></span>
  </div>
  <div class="experience__data bd-grid grid px-5">
    <h3 class="font-semibold mb-1">{title}</h3>
    <span class="font-light text-sm ">{subtitle}</span>
    <span class="font-light text-sm ">{honors}</span>
    <span class="font-light text-sm ">{average}</span>
    <p class="my-2 text-justify text-slate-600">
      {desc}
    </p>
  </div>
</div>
